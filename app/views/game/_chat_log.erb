<% for message in game.messages.last(10) %> 
  <% if message.user == "Red" %>
    <div id="chat-log" class="card text-white bg-danger mb-3 " style="max-width: 10rem;">
  <% elsif message.user == "Blue" %>
    <div id="chat-log" class="card text-white bg-info mb-3" style="max-width: 10rem;">
  <% else %>
    <div id="chat-log" class="card text-white bg-warning mb-3" style="max-width: 10rem;">
  <% end %>
      <div class="card-header">
        <%= message.user %>
      </div>
      <div class="card-body">
        <%= message.text %> 
      </div>
    </div> 
<% end %>