<div class="container">

  <div class="row justify-content-center">
    <h1>Pente</h1>
  </div>

  <%= render "game_headings", :game => @game, :session_id => session.id.to_s %>

  <div class="row">
    <div class="col-lg-9">
      <%= render "board", :game => @game, :session_id => session.id.to_s %>
    </div>
    <div class="col-lg-3 chat">
      <%= render "chat", :game => @game, :session_id => session.id.to_s %>
    </div>
  </div>
</div>