<% if session.id.to_s == @game.red_player %>
  <h2 class="text-center">Red Player</h2>
<% end %>
<% if session.id.to_s == @game.blue_player %>
  <h2 class="text-center">Blue Player</h2>
<% end %>

<%= render "board", :game => @game %>